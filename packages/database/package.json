{
  "name": "@ventureflow/database",
  "version": "0.1.0",
  "description": "VentureFlow AI - Database & Prisma Schema",
  "private": true,
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:migrate:dev": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:seed": "node --loader ts-node/esm seed.ts",
    "db:reset": "prisma migrate reset",
    "db:push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^5.7.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prisma": "^5.7.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
